[package]
name = "mhome-host-api"
version = "0.1.1"
edition = "2021"
license = "MIT"
repository.workspace = true
description = "Host machine observation wire contracts"
[dependencies]
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"
