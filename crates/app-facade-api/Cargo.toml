[package]
name = "mhome-app-facade-api"
version = "1.8.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Canonical public App Facade protocol for MeowLink clients"
readme = "README.md"
publish = ["crates-io"]
include = ["src/**", "tests/**", "schema/**", "fixtures/**", "manifest/**", "Cargo.toml", "README.md", "LICENSE"]

[lib]
name = "app_facade_api"

[dependencies]
artifact-api = { package = "mhome-artifact-api", version = "=1.2.0", path = "../artifact" }
conversation-api = { package = "mhome-conversation-api", version = "=1.2.0", path = "../conversation-api" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
jsonschema = { version = "0.30", default-features = false }
