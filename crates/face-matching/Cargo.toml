[package]
name = "mhome-face-matching"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Deterministic face gallery matching without inference or IO"
[lib]
name = "face_matching"
[dependencies]
person-api = { package = "mhome-person-api", version = "=0.1.0", path = "../person-api" }
